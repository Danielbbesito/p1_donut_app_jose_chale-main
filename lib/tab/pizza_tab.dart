import 'package:flutter/material.dart';

class PizzaTap extends StatelessWidget {
  const PizzaTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PizzaTap"));
  }
}